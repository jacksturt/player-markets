{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd fillFeed && npm install"
  },
  "deploy": {
    "startCommand": "cd fillFeed && npm run start:feed",
    "healthcheckPath": "/metrics",
    "healthcheckTimeout": 100
  }
}